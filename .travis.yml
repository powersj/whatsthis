language: python
dist: xenial
python:
  - 3.5
  - 3.6
  - 3.7
install:
  - pip install -r requirements.txt -r requirements-test.txt
script:
  - tox
  - python setup.py install
  - whatsthis version
  - whatsthis features
